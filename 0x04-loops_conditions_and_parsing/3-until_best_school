#!/usr/bin/env bash
# This script displays "Best School" 10 times using an until loop

# Initialize counter
counter=0

# Use until loop to repeat until counter reaches 10
until [ $counter -eq 10 ]; do
    echo "Best School"
    counter=$((counter + 1))
done
